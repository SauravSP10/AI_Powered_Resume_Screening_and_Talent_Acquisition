# Core dependencies
scikit-learn==1.3.0       # TF-IDF and similarity computation
spacy==3.5.3              # NLP preprocessing
en-core-web-sm            # SpaCy English language model
pdfplumber==0.9.0         # Extract text from PDFs
python-docx==0.8.11       # Extract text from DOCX files
numpy==1.23.5             # For numerical computations

# Optional for additional NLP capabilities
nltk==3.8.1               # Natural language toolkit

# Optional for OCR (scanned resumes)
pytesseract==0.3.10       # Optical Character Recognition (OCR)
Pillow==9.5.0             # Image handling for OCR

# Optional for data storage
pandas==1.5.3             # Tabular data handling
